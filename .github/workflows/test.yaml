name: Tests

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@main
    permissions:
      contents: write
      pull-requests: write
    secrets: inherit
    with:
      with-uv: true
