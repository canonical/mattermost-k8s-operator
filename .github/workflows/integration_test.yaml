name: Integration tests

on:
  pull_request:

jobs:
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@add-build-args
    secrets: inherit
    with:
      pre-run-script: tests/integration/localstack-installation.sh
      extra-arguments: '---localstack-ip 172.17.0.1 --mattermost-image="localhost:32000/mattermost:test"'
